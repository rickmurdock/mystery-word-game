<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Mystery Word Game</title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="style.css" rel="stylesheet">
  </head>
  <body>
    <div id="wrapper">
      <h1 id="mystery">
        <div class="tilt-left">M</div>
        <div class="tilt-right">y</div>
        <div class="tilt-left">s</div>
        <div class="tilt-right">t</div>
        <div class="tilt-left">e</div>
        <div class="tilt-right">r</div>
        <div class="tilt-left">y</div>
        <div class="tilt-right">&nbsp;&nbsp;</div>
        <div class="tilt-left">W</div>
        <div class="tilt-right">o</div>
        <div class="tilt-left">r</div>
        <div class="tilt-right">d</div>
        <div class="tilt-left">&nbsp;&nbsp;</div>
        <div class="tilt-right">G</div>
        <div class="tilt-left" id="cheat">a
          <span class="cheatText">{{game.word}}</span></div>
        <div class="tilt-right">m</div>
        <div class="tilt-left">e</div>
      </h1>
      <!--<p class="cheatText">{{game.word}}</p>-->
      <br>
      <br>
      <div id="letter-wrapper">
        {{#game.display}}
          <div class="letters">{{.}}</div>
        {{/game.display}}
      </div>
      <br>
      {{^game.lose}}
        <p id="guess-left">You have {{game.guessesLeft}} guesses left.</p>
      {{/game.lose}}
      <!--<p id="guess-left">You have {{game.guessesLeft}} guesses left.</p>-->
      {{#game.lose}}
        <p id="guess-out">You are out of guesses.</p>
      {{/game.lose}}
      <br>
      <form action="/" method="post">
        {{#game.playing}}
          <input type="text" name="guessLetter" placeholder="Enter a letter" maxlength="1" size"1" autofocus>
        {{/game.playing}}
        <!--{{^game.lose}}
          <p id="win">{{game.status}}</p>
        {{/game.lose}}-->
        {{#game.lose}}
          <p id="lose">{{game.status}}</p>
        {{/game.lose}}
        <input type="submit" value="{{game.btnText}}">
        <br>
        {{#game.playing}}
          <p id="guesses">Letters guessed: {{game.lettersGuessed}}</p>
          <br>
          <p id="message">{{game.message}}</p>
        {{/game.playing}}
        <br>
        {{^game.playing}}
          <input type="radio" name="mode" value="easy" checked> Easy: words of 4-6 characters<br>
          <input type="radio" name="mode" value="normal"> Normal: words of 6-8 characters<br>
          <input type="radio" name="mode" value="hard"> Hard: words of 8+ characters
        {{/game.playing}}
      </form>
    </div>
  </body>
</html>